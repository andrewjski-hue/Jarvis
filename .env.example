ANTHROPIC_API_KEY=your-api-key-here
PORT=3000
# Optional: path to a specific Chromium executable for the web browsing
# tools. Leave unset to use Playwright's own managed browser (recommended;
# install it with `npx playwright install chromium`).
# PLAYWRIGHT_CHROMIUM_PATH=
